# Anthropic API Configuration
ANTHROPIC_API_KEY="your-api-key-here"

# Environment (dev or prod)
# dev  = Uses Claude Haiku (fast, cheap: $0.25/1M tokens)
# prod = Uses Claude Sonnet 4 (accurate: $3/1M tokens)
ENVIRONMENT="dev"
